const errorMsg = document.querySelectorAll(".errormsg button");
const email = document.querySelector;

// const error = async () => {
//   const errormsg = await fetch("/signup");
//   if (errmsg) {
//     console.log(errormsg);
//   }
// };
